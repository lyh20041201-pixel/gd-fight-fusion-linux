# Linux 续训交接要求

用户已要求把这轮训练迁移到本 Linux 服务器，并让本端 Codex 下载后自动继续。
当用户要求执行本交接时，完成实际下载、校验、配置和启动，不要只返回命令或再次询问是否开始。
工作范围是此实验及用户提供的服务器资源；缺少账号登录、依赖安装权限或资源时报告具体阻塞。

## 执行顺序

1. 阅读根目录 README.md、release-manifest.json。检查本机 OS、GPU、Python 3.12、`gh auth status`、`tmux` 和持久磁盘空间。
2. 尊重用户指定工作目录/GPU；未指定 GPU 时默认物理 GPU 0。工作目录应在足够大的持久盘；不存在合适目录时只询问目录这一缺失信息。
3. 检查已有 `gd-fight-train` tmux 会话、控制器状态及训练进程，禁止并发启动重复队列。
4. 运行 `python3 tools/bootstrap.py --start --workdir <工作目录> --gpu <单个GPU>`。
5. 查看控制器日志、状态和四分支 GPU 冒烟结果。下载/安装仍在进行时如实报告对应阶段。
6. 持续等待并检查，直到正式队列运行且分支日志或进度确实推进，才报告“已启动训练”。报告 tmux 会话、目录、当前分支、种子和轮次。
7. 全部完成只能由正式队列 `status=complete`、REPORT.md 和三个种子的融合结果共同确认。测试集不能提前访问。

## 必须保持

- 使用 Release 中的封存包；`snapshot/` 只供审核，不能把它覆盖到正在训练的目录。
- 保留原始样本、标注、来源组和划分、种子 42/43/44、学习率、阶段轮数、早停和模型选择规则。
- 已完成的 global/roi seed42 自动跳过；随机骨架从已保存的第 9 轮、401/2819 游标继续。
- 不通过改哈希、删 selection/config/migration 文件或重置断点绕过校验。
- 不擅自更换 PyTorch/CUDA、放宽内存保护或改变测试封存规则。硬件不兼容时先报告实际 GPU 与失败日志。
- 不需要任何多模态云 API 密钥，不上传服务器凭据，不在 GitHub Actions 上训练。
- 不因 CPU 检查通过就宣称 GPU 可用，不因 tmux 创建成功就宣称训练开始。
- 内存或磁盘不足时报告原因，不自动重启形成循环，不删除数据和模型腾空间。

## 故障处理

允许修复明确的操作环境问题，例如在已有权限范围内安装 README 列出的依赖、选择正确的已有 Python 或使用用户授权的数据盘。
重试前确认旧队列已退出，继续使用同一工作目录；有效分卷和断点应复用。
需要停止训练时，只对 `linux_guard_status.json` 指向且命令行验证属于本实验的监护进程发送 SIGTERM，等待其停止本次队列和子进程。
保留日志与原子断点，报告最后保存位置与可能需要重跑的未保存工作。
